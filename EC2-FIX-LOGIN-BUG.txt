# Fix login authentication bug on EC2

cd ~/mushqila

# Pull latest code
git pull

# Rebuild web container with fixed code
docker-compose -f docker-compose.traefik.yml build web

# Restart containers
docker-compose -f docker-compose.traefik.yml up -d

# Wait
sleep 10

# Check logs
docker logs mushqila_web --tail=30

# Test admin login at: https://mushqila.com/admin/login/
# Username: mushqiladac
# Password: Sinan210@
